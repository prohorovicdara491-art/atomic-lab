# Atomic Lab - Практическая работа

## 1. Скриншот git log (демонстрация атомарности)
git log --oneline --graph

text

**Результат:**
f298198 chore: add pre-commit config and Makefile

ea490f0 test: add unit tests with Given-When-Then structure

b80c06c chore: fix file endings with pre-commit

02af128 fix: remove unused variable in main function

35b36ed chore: add .gitignore

0a73434 chore: fix dependencies versions

fa41d41 fix: add error handling to auth

9c74a00 feat: add user status function

e7488e5 refactor: rename variables for clarity

cb9b070 docs: add analysis of atomicity risks

9dc9c14 Initial setup

text

**Вывод:** Все 11 коммитов атомарные - каждый содержит только одно логическое изменение.

---

## 2. Чистый старт (восстановление окружения)

**Процесс:**
deactivate
rm -rf venv
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.lock.txt
python app.py

text

**Результат:**
Processing user: John (status: active)
Done

text

**Вывод:** Приложение успешно восстановилось из lock-файла.

---

## 3. Pre-commit хук (блокировка коммита)

**Пример блокировки:**
$ git commit -m "test"
black....................................................................Failed

hook id: black

files were modified by this hook

text

**Вывод:** Pre-commit успешно блокирует коммиты с плохим стилем кода.

---

## 4. Ответы на контрольные вопросы

### Вопрос 1: Как использование git add -p помогает при отладке через git bisect?

**Ответ:** git add -p позволяет создавать атомарные коммиты, где каждый коммит содержит только одно изменение. При использовании git bisect для поиска бага это помогает точно определить, какой именно коммит вызвал проблему, вместо того чтобы разбираться в коммите с множеством изменений.

### Вопрос 2: Почему наличие requirements.lock.txt критично для командной работы?

**Ответ:** requirements.lock.txt фиксирует точные версии всех зависимостей. Это обеспечивает:
- У всех разработчиков одинаковые версии библиотек
- Предсказуемую работу на серверах CI/CD
- Возможность восстановить окружение через любое время
- Защиту от неожиданных обновлений, которые могут сломать код

### Вопрос 3: В чем преимущество Makefile перед текстовой инструкцией в README?

**Ответ:** Makefile автоматизирует процессы и служит исполняемой документацией. Вместо того чтобы вручную вводить несколько команд, достаточно одной команды, например `make install`. Это уменьшает количество ошибок и унифицирует процесс для всех разработчиков.

### Вопрос 4: Как тесты реализуют принцип «живой документации» и почему фиксация seed важна?

**Ответ:** Тесты в стиле Given-When-Then показывают, как использовать функции и что они должны возвращать. Фиксация seed (random.seed(42)) делает тесты детерминированными - они всегда дают одинаковый результат, что важно для воспроизводимости.

### Вопрос 5: Что произойдет, если удалить папку venv и выполнить make install на чистой машине?

**Ответ:** make install создаст новое виртуальное окружение и установит все зависимости из requirements.lock.txt. В результате получится полностью идентичное окружение, и приложение будет работать так же, как на исходной машине.

---

## Итоги работы

В ходе практической работы были созданы:
- 11 атомарных коммитов
- Виртуальное окружение с фиксированными зависимостями
- Makefile для автоматизации
- Pre-commit хуки для проверки качества кода
- 8 успешных тестов
- Полный отчет с ответами на вопросы
